kind: pipeline
type: docker
name: webpage

workspace:
  path: /runner

steps:
  - name: build
    when:
      branch:
        - publish
    image: node
    volumes:
      - name: webpage
        path: /runner
    commands:
      - npm install
      - npm run build
      - cp ./build /runner/webpage
volumes:
  - name: webpage
    host:
      path: home/drone/data/webpage
