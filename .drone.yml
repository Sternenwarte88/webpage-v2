kind: pipeline
type: docker
name: webpage

steps:
  - name: build
    when:
      branch:
        - publish
    volumes:
      - name: webpage
        path: /webpage
    image: node
    commands:
      - npm install
      - npm run build
      - copy ./build ./webpage

volumes:
  - name: webpage
    host:
      path: /data
