kind: pipeline
type: docker
name: webpage

steps:
  - name: build
    when:
      branch:
        - publish
    image: node
    volumes:
      - name: webpage
        path: /webpage
    commands:
      - npm install
      - npm run build
      - copy ./build ./webpage

# volumes:
#   - name: webpage
#     host:
#       path: data
