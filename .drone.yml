kind: pipeline
type: docker
name: webpage

steps:
  - name: build
    when:
      branch:
        - publish
    image: node
    volumes:
      - name: webpage
        path: /tmp/cache
    commands:
      - npm install
      - npm run build
      - cp ./build /runner/webpage
volumes:
  - name: webpage
    host:
      path: home/drone/data/webpage
