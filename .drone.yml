kind: pipeline
type: docker
name: webpage

steps:
  - name: build
    image: node
    volumes:
      - name: cache
        path: /tmp/cache
    commands:
      - npm install
      - npm run build
      - cp ./build -R /runner

volumes:
- name: cache
  host:
    path: /home/drone/data/webpage
